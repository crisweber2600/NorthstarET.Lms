# Contract Test Skeleton: CreateTenant

```csharp
public class CreateTenantContractTests
{
    [Fact]
    public async Task CreateTenant_ShouldReturn201_WithTenantPayload()
    {
        // Arrange: generate request payload using OpenAPI example
        // Act: invoke POST /v1/tenants against stub server (will fail until implemented)
        // Assert: status code == 201 and response matches schema defined in roster-api.v1.yaml
        // Current status: EXPECTED TO FAIL (Not yet implemented)
        Assert.Fail("Contract stub pending implementation");
    }
}
```
